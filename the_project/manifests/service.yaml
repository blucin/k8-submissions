apiVersion: v1
kind: Service
metadata:
  name: the-project-service
spec:
  type: NodePort
  selector:
    app: the-project
  ports:
    - name: http
      nodePort: 30080
      protocol: TCP
      port: 1234
      targetPort: 8080
