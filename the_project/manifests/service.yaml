apiVersion: v1
kind: Service
metadata:
  name: the-project-service
spec:
  type: ClusterIP
  selector:
    app: the-project
  ports:
    - name: http
      protocol: TCP
      port: 1234
      targetPort: 8080
